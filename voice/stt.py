"""
stt.py — Speech-to-Text (placeholder para etapa futura).

Nesta etapa o STT não está ativo. Este arquivo existe para estruturar
o módulo que será implementado com Whisper na próxima fase.
"""


def transcribe(audio_path: str) -> str:
    """
    Transcreve um arquivo de áudio para texto usando Whisper.

    [NÃO IMPLEMENTADO - Fase 2]

    Args:
        audio_path: Caminho para o arquivo de áudio.

    Returns:
        Texto transcrito.
    """
    raise NotImplementedError(
        "STT com Whisper será implementado na Fase 2 (módulo de voz)."
    )
